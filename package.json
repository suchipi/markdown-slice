{
  "name": "markdown-slice",
  "version": "0.1.0",
  "description": "Print a specific subset of a markdown document by using query selectors",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && tsc && chmod +x dist/cli.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@suchipi/esquery": "^1.0.1",
    "clefairy": "^0.4.0",
    "get-stdin": "^9.0.0",
    "gray-matter": "^4.0.3",
    "remark": "^14.0.3"
  },
  "devDependencies": {
    "first-base": "^1.3.0",
    "typescript": "^5.1.3",
    "vitest": "^0.31.4"
  },
  "keywords": [
    "markdown",
    "query",
    "selector",
    "slice"
  ],
  "author": "Lily Skye <me@suchipi.com>",
  "license": "MIT"
}
